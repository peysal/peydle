/**
 * Build script to ease the pain.
 */

def rootWorkingFolder = [location :new File(".").absolutePath]
def orchestra = [location: ""]
def dataRuntime = [location: ""]
def executionClient= [location: ""]
def testFolder = [name : "testFolder"]
def masterConfig = [rootWorkingFolder, orchestra, dataRuntime, executionClient]

task myConfig << {
    def header = "---My config---"
    println header
    masterConfig.each { config ->
        println config
    }
    header.size().times {print "-"}
}

task runTest << {
    def header = "---Commence run test---"
    println header
    def rootFolderOfTest = new File("$rootWorkingFolder/$testFolder.name")
    if(rootFolderOfTest.mkdirs()) {
        println "successfully created:$testFolder.name"
    }
    header.size().times {print "-"}
}

task cleanTest << {
    def header = "---Clean test---"
    println header



    header.size().times {print "-"}
}


